## Agent skills

### Issue tracker

Issues and PRDs for this repo live as markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The canonical triage roles map directly to the default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context layout: `CONTEXT.md` at the repo root and `docs/adr/` for architecture decisions. See `docs/agents/domain.md`.
