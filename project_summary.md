# MCRebuild V2 项目总结（2026-08-17 发布版）

Status: released（v2.0.0 正式版候选）

本文是本轮 V2 重写开发的收口总结：最终状态、交付范围、以及开发过程中沉淀的经验教训。
产品行为以 `docs/product-baseline.md` 为准；架构决策以 `docs/adr/` 为准；施工历史见
`.scratch/v2-implementation/`。

## 一、最终状态

- 版本：**v2.0.0**（从 `2.0.0-dev` 收敛；正式版候选）
- 分支：`main`，HEAD `7988d5d`（已推送 `origin/main`）
- 工作树：主工作区干净；已合并分支与过期工作树已清理
- 门禁：workspace 测试全绿 / fmt --check / clippy -D warnings / machete 通过
- 全部 A–H 阶段工单与主线 M1–M5 收口项已完成并验收

## 二、交付范围（用户可见）

1. 首启向导：语言 + Minecraft 版本 + 高德 API Key/安全密钥（必填，ADR-0004）+
   Web 服务 Key（选填，开发人员使用），第一页配置齐所有必要项。
2. 校区搜索：高德在线搜索真实校区 → 建方案 → 五步工作区。
3. 边界：OSM 自动获取 → 顶点编辑（选中/拖动/加号插入/删除按钮）→ 确认；
   边界变更后候选资格本地重验证（不联网），边界外候选回隔离、旧评审决定作废留痕。
4. 候选采集：六类（建筑/道路/水体/植被/体育/其他），严格按确认边界筛选，
   原始证据入库，报告含 来源/边界内/相交/边界外/无效/最终 Reviewable。
5. 评审：抽屉化六类标签 + 地图定位/标注 + 三态判定 + 批量操作 + 封账 +
   轻量建议辅助（未命名/重叠/形状可疑筛选，一键应用，撤销上一批）。
6. 导出：边界直出基础 `.schem`；增强导出含保留候选；失败明确回退不伪造。
7. 导航：顶部工具栏（左上返回+上下文标题、右上通知/设置、回收站/切换校区进溢出菜单），
   全局逐层返回（历史栈），返回工作区恢复方案与步骤。
8. 稳定性：边界/候选抓取超时与端点回退有明确反馈；WebView 加载不再透出桌面；
   工作现场跨重启恢复（边界/朝向/未封账评审决定）。

## 三、经验教训

### 1. 超时加长不是根因修复
抓取“时快时慢甚至报错”的真实原因是公共 Overpass 端点负载与级联回退，而不是超时太短。
修复要先记录每阶段耗时（校名解析 / 每端点 / 解析），用证据定位，再决定端点策略与
用户反馈，而不是一味把超时调大。

### 2. 会话缓存 ≠ 持久化
“切步骤不再重复请求”解决了会话内体验，但重启即丢。要支持“完全恢复上次工作现场”，
必须写库，且状态变更即落库（write-through），才能在意外退出后恢复。

### 3. 边界资格是本地计算，不该依赖网络
原始观测几何已入库，换边界后重新判定“是否在边界内”是纯本地运算。
“换边界后重采集必现网络错误”是重采集路径每次都重新拉 Overpass 的 bug，
而不是资格判定本身需要网络。

### 4. 单一数据真相源
候选资格（Reviewable/Isolated）应在写入侧（边界确认时）重验证并更新 B2，
而不是在评审台每次进入时按当前边界重算——否则评审显示与封账/导出读取的
投影会漂移。写时更新优于读时重算。

### 5. 导航层级要按惯例，不要“想怎么摆就怎么摆”
官方规范（Fluent/HIG/Material/Carbon）一致支持：≤5 个同级顶级入口用顶部导航；
左上角返回 + 上下文标题；右上角高频操作；低频操作收进溢出菜单；返回用历史栈
逐层退。详见 `docs/research/nav-workspace-layout-forms.md`。

### 6. WebView 透明闪烁要设背景色
WebView2 默认背景透明，页面未绘制时透出桌面。创建时设不透明背景色即可消除。

### 7. 大 WIP 要逐 hunk 移植，禁止整批 cherry-pick
旧 C 未提交的大 WIP 混合了可用实现与禁止项（数据库迁移、审核/导出耦合、接口断裂），
必须按工单逐 hunk 评估复用，否则把已知失败一起带进来。

### 8. 缓存与构建纪律
`CARGO_BUILD_JOBS=2`、`SLINT_BACKEND=software`、不跨工作树共享 target、
开工前/后跑 `cargo xtask cache-report`、禁止 `cargo clean`。共享 target 会污染
并行工作树的测试结果。

### 9. 一工单一工作树，并行要划文件所有权
主线阶段串行推进；确需并行时，先分析共享文件（如 `workspace_adapter.rs`、
公共测试），划分文件所有权并顺序合入，避免合入冲突与验收互相污染。

### 10. 工单状态要及时回填
多个 T 系列 issue 文件停留在 `Status: ready-for-agent`，实际已实现并合入。
收口时必须回填状态，否则“已完成工单”看起来仍是待办。

## 四、文档地图

- 产品行为：`docs/product-baseline.md`
- 架构决策：`docs/adr/`（0001–0044）
- 施工顺序与状态：`.scratch/v2-implementation/v0.1-end-to-end-mainline-plan.md`
- 工单：`.scratch/v2-implementation/issues/`
- 验收记录：`docs/developer-guide/m5-e2e/manual-acceptance-record.md`
- 研究：`docs/research/`（含 nav-workspace-layout-forms.md）

