[package]
name = "campus-services"
version.workspace = true
edition.workspace = true

[dependencies]
campus-state = { path = "../campus-state" }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

