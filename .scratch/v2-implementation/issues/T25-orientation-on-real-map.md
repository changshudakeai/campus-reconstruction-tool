# T25 — 朝向参考线搬上真地图 + per-plan 收尾

> 法源声明：与工单描述冲突时以 ADR 为准。重点条款：ADR-0012（设定朝向 =
> 高德地图上画两点参考线，既定交互不变）、ADR-0007（边界/朝向属于方案）、
> ADR-0027（步骤条门控）、ADR-0021（弹窗铁律）、ADR-0017（壳零业务逻辑，
> 角度计算全委 B5）。
> 规格来源：`.scratch/v2-implementation/PRD-gaode-map-integration.md`；
> 地图基建：T24 交付的 `map_webview.rs`（show/hide/evaluate_script/IPC）与
> `boundary_edit_map_page.rs` 生成的地图页，本单在其上扩展朝向模式。

**What to build（负责人视角）：**
负责人在屏 4 第②步，在同一张真地图上点两个点，画出一条朝向参考线（两点
模式）；也可以切换回输入方位角的方式（保留）。朝向确定后步骤条第②格打勾、
第③格解锁；已有朝向时重设，仍先弹重算确认窗（确认应用新值 / 取消回滚）。
每个方案的朝向各归各：A 方案设好朝向，切到 B 方案看不到 A 的参考线，
切回来还在（按方案隔离，消解"朝向 UI 状态全局"旧债）。

**Blocked by：** T24 ✅（圈边界上真地图，地图基建与 IPC 协议已就绪）。

**Status：** completed（2026-07-28 commit ef9cd52：真地图朝向+per-plan 收尾）

## 验收标准

- [ ] 地图上点两个点画出朝向参考线，角度经 B5 计算（壳零计算，ADR-0017）；
  参考线在地图上可见（非只取点无可视化）
- [ ] 方位角输入模式保留，两种模式可切换（沿用 T19B-5B Step B 交互）
- [ ] 朝向 UI 状态 per-plan：A 方案设朝向后切 B 方案不串，切回 A 仍在
  （纳入 PlanProgressState 同一模型）
- [ ] 重算确认弹窗闭环沿用：确认应用新值、取消回滚
- [ ] 步骤条门控复验：新方案未设朝向时点第③格 = 灰色不可点；卡片文案
  与步骤条读同一份进度数据（单一事实来源）
- [ ] Step B 遗留两项破坏性测试并入本单验收：画自相交多边形点确认弹报错
  （非闪退）；新方案未设朝向第③格灰色
- [ ] B5 朝向相关行为单测（两点 → 角度 → 参考线端点换算）

## 通用收工标准

- [ ] 全部门禁绿灯：cargo fmt / clippy / test / machete / deny / xtask ci +
  GitHub Actions run success
- [ ] 文案键独立 commit（zh-CN.json 高冲突铁律）；.slint 双零 grep 取证
- [ ] 验收逐条对照代码证据打勾
- [ ] 占位项诚实声明写入完成汇报
