# T27 — 边界/朝向进度持久化（F3/B2 扩展，另立）

**Status:** historical（2026-08-17 v2.0.0 发布收口；不具独立开工权）

> 来源：T19B-5B Step C 诚实声明的占位项——进度状态只在内存，应用重开后
> 归零；负责人已确认归属"另立工单"，不进高德接入批次
> （PRD-gaode-map-integration.md 附注 · 技术债归属）。
> **Status：backlog——暂不动工，待高德批次完成后排期。**

**What to build：**
方案的边界 / 朝向完成状态（Step C 的 PlanProgressState，高德批次后扩展为
经纬度边界点与朝向数据）持久化到数据库：应用重开后步骤条打勾、卡片进度
文案、已圈边界与朝向参考线全部恢复。涉及 B2 持久化层扩展（方案级字段）与
F3 方案管理读写，壳只经既有缝取用。

**Blocked by：** 建议排在 T25 之后（高德批次会把边界点改成经纬度格式，
提前做持久化等于做两遍）。

**Status：** backlog

## 验收标准（排期时细化）

- [ ] 应用重开后 per-plan 进度（步骤条 / 卡片文案 / 边界 / 朝向）完整恢复
- [ ] B2 扩展只经公开 trait，F3 不触碰 SQL（ADR-0017）
- [ ] 旧版本数据库打开不崩（迁移或兼容策略明确）
- [ ] 通用收工标准（门禁 / CI / 双扫 / 诚实声明）
