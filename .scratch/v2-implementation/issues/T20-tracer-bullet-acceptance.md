# T20 — 贯穿弹集成验收（最小可导出通道端到端测试）

**What to build:** 打通全流程的最细通道验收——建方案 → 采一小块 → 评审一条 → 导出迷你 .schem，双击"开发版"快捷方式，非技术负责人亲手走通四步剧本。

- **测试缝**：PRD.md 定义的最高验收缝——候选夹具（fixture）标为保留 → 断言产出合法 .schem 与如实 manifest；集成测试贯穿 B1/B18/B4/B17 不经过壳。
- **通道剧本**：① 首次设置页选校区 → 方案列表新建方案；② 圈一小块边界 + 两点定朝向 → 采集拉回十来栋楼全部"待定"；③ 评审台改一条为"保留"；④ 导出确认弹窗（保留 1 项/N 项待定/封账后果）→ 进度条 → .schem + manifest 落盘。
- **不允许薄的纪律**：三层锁执法、文本外置、弹窗铁律、数据粮仓落库、封账语义、用料表版本绑定——第一天就得是对的。

**Blocked by:** T04-T19（全部功能模块 + 壳就绪）

**Status:** historical（2026-08-17 v2.0.0 发布收口；不具独立开工权）

- [ ] 集成测试实现：候选夹具 → 保留一条 → 断言 .schem 合法（NBT 结构 + 方块数 ≥1）
- [ ] 集成测试实现：断言 manifest 如实记录"只含建筑，其余类别缺失"
- [ ] 端到端 UI 流程验证脚本或手动清单（四步剧本逐步核对）
- [ ] 弹窗铁律验证：导出确认弹窗包含类别汇总 + 封账后果 + 待定报数三要素
- [ ] 数据粮仓验证：导出后查库确认原始观测数据仍在
- [ ] 封账验证：确认后评审入口禁用、评审状态不可再改
- [ ] 用料表版本绑定验证：请求不存在于目标版本的方块必须报错而非替换
- [ ] 文本外置抽查：随机抽 10 处界面文字确认全部来自资源文件

---

## 负责人验收点（一句话）

双击"校园复刻工具 - 开发版"，亲手走完建方案 → 圈边界定朝向 → 采集 → 保留一栋楼 → 导出四步，最后拿到一个 .schem 文件和一份如实写着"只含建筑"的清单——全程文字通顺、点错能改、要紧事都弹窗。

