# T19B-5A — 还债与基建（色卡 + 对话框 + ADR-0010/0018 违规修复）

**What to build**: 不盖新楼，只还 ADR 欠账 + 立两大基建。三笔欠账（色卡机制 / 轻创建对话框 / ···菜单+相对时间）
全部修复，并落地"色卡资源文件"与"通用对话框"两项基建，供后续所有工单复用。
另含第 0 号收尾任务：把 T19B-5 会话遗留的未提交改动按规矩提交推送拿绿灯。

- **法源**: ADR-0023（色卡机制，§一/§二/后果节）、ADR-0010（轻创建）、ADR-0018（···菜单/相对时间，§一/§三）。
  工单描述与 ADR 冲突时以 ADR 为准。
- **由来**: 2026-07-27 代码 vs ADR 全量审计查出三笔欠账，负责人批准"先还债再盖楼"（新执行顺序第 1 位）。

**Blocked by**: T19B-4（已完成）

**Status**: completed（2026-07-27）

## ⓪ 前置任务：第 0 号修雷成果提交（开工第一件事）

- [x] `git status` 应有未提交改动：`apps/desktop/ui/campus_select.slint`（伪 ID 按钮已换成 F3 动态真列表 +
  "新建演示校区"按钮）、`apps/desktop/src/injector.rs`、`apps/desktop/ui/main.slint`、
  `core/localization/resources/zh-CN.json`、`docs/adr/0027-stepper-wizard-navigation.md`（第 6 轮子决策补录）。
  改动本身已经负责人核验合格，按规矩提交：
  - [x] zh-CN.json 改动**单独 commit**（文案铁律）
  - [x] ADR-0027 文档改动单独 commit（docs 类）
  - [x] 其余代码改动一个 commit
  - [x] 推送后 GitHub Actions conclusion=success，再继续后面的任务
- [x] 若工作树干净，说明已被处理——用 `git log` 核实这批改动确已入库后再继续，不许臆断

## 🎯 验收标准

### 一、色卡基建【ADR-0023 §一 硬性架构约束】

- [x] 建立色卡资源文件与"颜色角色名"机制：代码**永远不写颜色号，只写颜色角色名**（ADR-0023 §一原文）。
  角色按现有四屏实际用色归纳，至少覆盖：背景色（现 #ffffff）、主文字（#222222）、次要文字（#666/#888）、
  弱文字（#999/#aaa）、分隔线/按钮底（#e0e0e0）、气泡底（#fffde7）、气泡边框（#fbc02d）、警示色（#c62828）
- [x] 现有全部 .slint 文件的十六进制颜色号一律替换为角色名取色；
  完工后 `grep '#[0-9a-fA-F]'` 在 apps/desktop/ui/*.slint **零命中**（色卡文件除外）
  （2026-07-27 实扫基线：campus_select 2 处、main 4 处、plan_list 7 处，共 13 处）
- [x] 机制必须做到"**新增一张色卡文件即可换肤，代码零改动**"（ADR-0023 §一："切主题 = 换色卡"）；
  色卡文件的存放与加载方式参考 core/localization/ 的 B6 资源加载模式
- [x] 本单只落**亮色卡**；暗色卡与设置页切换开关（ADR-0023 §二）如实标注为后续债务，记入 T19B-ui-connection.md 索引
  ——注意 ADR-0023 后果节"不存在先写死再迁移的过渡态"，因此角色名机制必须本单一步到位，只是暗色卡文件延后

### 二、对话框基建（通用零件，后续所有危险操作与输入场景复用）

- [x] 通用**确认窗**：标题+正文+确认/取消两钮，文案全部注入（调用方给文本键，零硬编码）
- [x] 通用**输入窗**：单行文本框（支持预填值）+确认/取消，文案全部注入
- [x] 两窗均经 ViewModelInjector 接线，回调错误递 report_callback_error（架构铁律）

### 三、ADR-0010 还债：新建方案改为轻创建对话框

- [x] 点击"新建方案"弹**输入窗**：预填可修改的默认名（ADR-0010 决定节："预填可修改的默认名，如'方案 1'"；
  预填值沿用现有自动命名逻辑），确认后创建
- [x] 同名冲突拦截：同一校区内方案名不得重复（ADR-0010 后果节校验规则），提示走 `plan.duplicate_name`
  （已核实在库：zh-CN.json plan 节）
- [x] ⚠️ 范围切割：ADR-0010"确认后……进入方案工作区"这半条**不在本单**——工作区屏归 T19B-5B，本单建完仍留列表页
  （此为诚实妥协，需在汇报中声明）

### 四、ADR-0018 还债：···菜单 + 相对时间

- [x] 卡片右侧三个平铺按钮收进"**···**"菜单（ADR-0018 §三）：一个 ··· 按钮，点开浮层菜单：改名/复制/删除
- [x] **改名**接通：···菜单 → 输入窗（预填现名）→ F3 rename_plan；同名冲突提示（ADR-0018 §三：沿用 ADR-0010 校验规则）
- [x] **删除**加二次确认：确认窗文案用 `plan.delete_confirm`（已核实在库："回收站保留 30 天"，符合 ADR-0018 §三删除行为规则）
- [x] **最后修改时间**改相对表述（ADR-0018 §一第 3 条："相对表述（如'3 天前'）"）：
  "刚刚 / X 分钟前 / X 小时前 / X 天前"，超 7 天显示日期；
  格式化逻辑放 Rust 侧（壳的展示层允许），文案键走 zh-CN.json 支持参数
- [x] 复制方案行为不动（T19B-4 已实现，新名"副本"后缀符合 ADR-0018 §三），仅入口挪进 ···菜单

### 五、文书同步

- [x] 更新本工单勾选项
- [x] 更新 T19B-ui-connection.md：三笔欠账标记已还 + 暗色卡与切换开关延后声明
- [x] 更新 apps/desktop/STATUS.md

## ⛔ 本单红线

- 不新增任何页面/屏号，不碰步骤条（归 5B）、不碰公告栏/回收站（归 T19B-9）
- 不引入任何新第三方依赖；不改 deny.toml / workspace.lints
- .slint 零硬编码中文、**零硬编码颜色号**（本单起新增门禁级红线，验收扫描项）
- 占位就写占位，"已打通"只准用于亲手运行验证过的路径
- 预期卡点：遇到就停，别硬闯——若发现需要引入新依赖（如浮层菜单需要第三方组件），停下报方案等负责人拍板

## ✅ 收工标准（缺一不可）

1. [x] 手动走通：新建方案弹输入窗（预填名可改、同名被拦）→ ···菜单改名成功 →
   删除先弹确认、取消不删、确认后进回收站 → 列表时间显示"X 天前"式
2. [x] grep 验证：ui/*.slint 无颜色号（色卡文件除外）、无硬编码中文
3. [x] 全套门禁：cargo test --workspace / xtask tidy / xtask arch /
   clippy --workspace --all-targets -- -D warnings / fmt --all --check /
   machete / deny check advisories bans licenses sources
4. [ ] git push origin main 后 GitHub Actions conclusion 绿灯——唯一完成标准（汇报附链接）
5. [x] 汇报列明：每笔 ADR 欠账对应的修复 commit；如有妥协逐条诚实声明

---

## 负责人验收点（一句话）

新建方案会先问名字（起好了可以改），卡片右边只剩一个"···"按钮（点开才见改名/复制/删除），
删除会再确认一次，时间显示"3 天前"这种人话——而且以后想换界面配色只要换一张色卡文件。
