# T13 — F4 数据采集（数据源适配器 + 增量刷新检测 + 标签映射 + 原始观测落库）

**What to build:** 采集非强制（可跳过），六类可选随时导出；从高德/Overture 等数据源拉取候选对象 → 归类 → 原始数据当场写库（永不删除）→ 供评审使用。

- **窗口契约**：缝 3（采集流水线 ↔ F4 → B12 → B13）。F4 向 B12 要原始对象 → B13 归类 → F4 把原始观测数据写入 B2；同时实现增量刷新检测（对比上次采集展示差异）。
- **业务规则**：查询标签范围须显著宽于 v1.x（泳池、围墙、校门、停车场、喷泉、纪念碑、树木等）；映射不上的归"其他"禁止静默丢弃；数据源可插拔同一 trait 多实现。
- **增量刷新**：对比上次采集的 digests/时间戳 → 展示差异（新增/更新/未变）。

**Blocked by:** T05（B3 高德客户端与边界交互）, T11（B2 原始观测表结构）, T12（B13 标签映射表与归类引擎）, T14（B5 边界/朝向数据）

**Status:** completed

> **ADR-0040 hardening（2026-08-01）**：既有采集/分类/原始落库能力保持完成，但 F4 还需返回带来源点/线/面语义的采集批次与候选草稿。当前高德 POI 只有点位，只能作为原始证据，不得据此伪造建筑轮廓；完成该契约前不得用本单旧状态证明 M3 候选链完整。

- [ ] 采集批次保留数据源原始几何种类、坐标和来源关系；无几何对象仍生成原始观测
- [ ] 向 A1 返回原始观测与候选草稿，不要求 A1 从任意 JSON 猜测几何
- [ ] 至少一个生产数据源真实提供建筑轮廓，供 M3“十来个可评审建筑”验收；测试夹具不得冒充生产联网能力

- [x] data-acquisition crate 立项并实现数据源适配器接口（DataSource trait）
- [x] gaode-client crate 集成到 F4（作为默认数据源实现）
- [x] 边界绘制后调用数据源 API 的参数传递逻辑（polygon → 原始对象列表）
- [x] 增量刷新检测逻辑框架（比较上次采集 digest vs 本次原始数据）
- [x] F4 向 B2 写入原始观测数据的 API 调用（数据粮仓逻辑）
- [x] 采集进度反馈 UI 占位（进度条显示当前类别进度）
- [x] public-api 快照测试 + 初始快照入库
- [x] 集成测试：给定一小块边界 → 断言采集回来的对象数 ≥1

---

## 负责人验收点（一句话）

圈完边界点采集，能看到"正在从地图平台拉数据……完成了 XX 个对象"，其中体育馆被标记为体育、校门被标记为其他。

