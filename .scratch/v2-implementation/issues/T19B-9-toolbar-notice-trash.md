# T19B-9 — 右上角四入口统一工具栏 + 公告栏页 + 回收站页

**What to build**: ADR-0027 第 4 轮子决策的"右上角四入口"在方案工作区各步骤页的统一落地——
公告栏页、回收站页，以及所有页面共用的右上角小图标区（公告栏 · 切换校区 · 设置）。本单实施这三张独立页面，
头部工具栏由 T19B-5B 在窗口容器层统一装配，各页只负责内容区域。

- **UI 决策法源**: ADR-0027 §决定（四入口清单固定右上角，任何页面位置不变；带文字标签，不使用抽屉/汉堡菜单）
  + ADR-0028 后果节（公告栏页顺手补 toast/铃铛两个 Presenter 空壳）
- **由来**: 新执行顺序第 3 位，排在 T19B-5B 之后（ADR-0027："每页都有工具栏，先建后面三张单出生自带"）

**Blocked by**: T19B-5B（完成五格步骤条与方案打开路径后，工具栏才有载体）

**Status**: completed（2026-07-27 commits 4f4148d/f04ed36/52952f5/6615277/20fbfcd；toast/铃铛债务另立）

## 🎯 验收标准

### 一、右上角工具栏（统一布局，T19B-5B 装配容器，本单复用此约定）

- [ ] 四入口固定右上角：公告栏 · 切换校区 · 设置，三个入口配文字标签（四个图标含一个···菜单？不对，
  回收站是独立页，这里只有三个图标——见下修正：实际为**公告栏 / 切换校区 / 回收站 / 设置**四样全上架，见 ADR-0027）
- [ ] 任何页面位置不变（可预测性优于省图标）；点击即跳各自目标页或弹出功能选择器
- [ ] 图标 + 标签样式走 5A 色卡角色名，零颜色号（延续门禁级红线）

### 二、公告栏页（`notice_board.slint`）

- [ ] 非紧急消息集中地（紧急事走弹窗铁律：ADR-0021）；消息列表、日期前缀、详情展开
- [ ] 消息持久化走 F3；Shell 不写业务逻辑，仅展示层
- [ ] 顺手补 **toast/铃铛** 两个 Presenter 空壳（presenter.rs 留注释，见 ADR-0028 后果节）
- [ ] 新增文案键走 l10n.t() 补充 zh-CN.json（notice.* 前缀），.slint 零硬编码中文

### 三、回收站页（`trash.slint`，校园级共享）

- [ ] **恢复操作**：单条恢复按钮；**恢复重名自动加“（恢复 1）”后缀**（沿用 v1.x 既有决策：零交互、不覆盖、不弹窗问用户）
- [ ] **立即永久删除**：在回收站内立即永久删除需再次确认（沿用 v1.x 决策）→ 用 5A 通用确认窗，文案复用 `plan.purge_confirm`
  （已核实在库："确定要立即永久删除吗？此操作不可恢复。"）
- [ ] 保留 30 天规则：界面如实显示删除时间/过期时间（相对表述，格式化逻辑放 Rust 侧，文案键支持参数）
- [ ] 按删除时间倒序（最近删的排最前）
- [ ] 新增文案键走 l10n.t() 补充 zh-CN.json（trash.* 前缀），.slint 零硬编码中文

### 文案与国际化

- [ ] notice.* / trash.* / settings.*（如需）统一走 l10n.t()；.slint 零硬编码中文
- [ ] 文案改动单独 commit（zh-CN.json 高冲突文件铁律）

### 架构断言（CI 门禁必过）

- [ ] `cargo xtask arch` 通过：shell 不依赖 B12-B16
- [ ] `cargo deny check bans` 无违规
- [ ] 一律经 ViewModelInjector 访问器接线；回调错误递 report_callback_error

## ⛔ 本单红线

- 不碰采集/评审/导出正文区域（分别归 T19B-6/7/8）
- 不引入任何新第三方依赖；不改 deny.toml / workspace.lints
- .slint 零硬编码中文、零硬编码颜色号
- 占位就写占位，"已打通"只准用于亲手运行验证过的路径
- 预期卡点：遇到就停，别硬闯——若发现需要引新依赖（如通知中心网络轮询等），停下报方案等负责人拍板

## ✅ 收工标准（缺一不可）

1. [ ] 手动走通：公告栏查看消息列表 → 回收站恢复一条（同名冲突流程）、立即永久删除一条（二次确认）
2. [ ] grep 验证：ui/*.slint 无颜色号（色卡文件除外）、无硬编码中文
3. [ ] 全套门禁：cargo test --workspace / xtask tidy / xtask arch /
   clippy --workspace --all-targets -- -D warnings / fmt --all --check /
   machete / deny check advisories bans licenses sources
4. [ ] git push origin main 后 GitHub Actions conclusion 绿灯——唯一完成标准（汇报附链接）
5. [ ] 汇报列明占位项清单（如有），逐条诚实声明

---

## 负责人验收点（一句话）

右上角那四个图标能跳转对地方；回收站里恢复一个方案、把一个误删的彻底清空（还会再问一次你）。
