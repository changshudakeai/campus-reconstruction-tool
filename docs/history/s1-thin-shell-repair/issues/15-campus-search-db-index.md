# 15 — 校区搜索改为数据库查询并加索引（backlog）

**来源：** s1-04 交付遗留（2026-08-01）。
**Status:** backlog

**What to build：** 当前校区搜索是 F3 内存过滤（名称包含匹配），数据量大时不可靠；改为数据库查询并为名称列建立索引。

**验收标准：**
- 搜索由 F3 公开接口执行数据库查询，结果与现行为一致（仅在点击搜索/回车时触发）
- 建索引走 B2 schema 迁移；不引入未批准依赖
- 文本外置、通知规则不变；S1 不直接触碰数据库（ADR-0037）
- 全门禁通过