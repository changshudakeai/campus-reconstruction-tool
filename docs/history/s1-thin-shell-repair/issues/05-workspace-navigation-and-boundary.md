# 05 — 迁移方案工作区、步骤导航与边界

**What to build:** 让方案工作区通过功能入口提供完整页面状态和导航决定，并让边界绘制、验证和保存由对应功能完成；S1 只显示五步流程和通用地图。

**Blocked by:** 02 — 建立新呈现入口并保留旧通道.

**Status:** merged（2026-08-01 提交 [S1-05] 并合并 PR #8，merge commit a69163c；待负责人验收）

- [x] 五个步骤顶部始终同时显示校区名和方案名。
- [x] 点击步骤或“下一步”后，由功能入口返回允许进入、条件不足或需要确认的决定。
- [x] S1 不自行判断边界是否完成、当前步骤是否可进入或缺少什么条件。
- [x] 地图呈现通道只负责显示地图和转交用户绘制动作，不包含边界业务规则。
- [x] 边界闭合、有效性、重置和保存由对应功能入口处理并返回完整结果。
- [x] 正常本地判断立即返回；需要加载地图时界面立即显示处理状态且不冻结。
- [x] 高德地图故障只暂停地图相关操作，设置、方案列表和已有正式数据仍可安全访问。
- [x] 离开边界页面时由功能入口判断可以离开、需要确认或必须停留。
- [x] 工作区导航与边界的现有用户可观察行为基线全部通过。
