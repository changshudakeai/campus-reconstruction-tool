# S1 / M3 规则冲突审计（2026-08-01）

## 判定顺序

1. 较新的已接受 ADR 修订较早 ADR。
2. `docs/module-decisions.md` 与当前 PRD 必须投影最新 ADR，不得另创规则。
3. 当前 `ready-for-agent` 工单必须服从前两项。
4. 已完成工单、handoff 和研究报告只记录历史；与最新 ADR 冲突时不得作为实施授权。

## 本轮发现

| 冲突或缺口 | 判定 | 文档处理 | 代码/门禁状态 |
|---|---|---|---|
| T19B-6 要求 S1 运行时先 F4 后 F7；ADR-0037 禁止 S1 业务协调 | ADR-0037 优先；ADR-0039 指定落点 | 改为 A1 `collection-flow` 完整入口 | 69b5b15 仍需 hardening |
| T19B-8 要求壳实现 `SealGate` 并调用 F5；ADR-0037 要求一次完整导出入口 | ADR-0037 优先 | 禁止继续按旧描述实施；M5 前另定独立导出应用流程模块，不能塞入 A1 | 当前仍为 M5 架构前置 |
| ADR-0017/0025 旧白名单、研究报告把 S1 写成“业务编排” | ADR-0037 已收紧旧授权 | ADR-0017、术语表、模块索引和研究报告均补充失效说明 | `xtask arch` 仍只检查依赖图，s1-10 必须补运行期编排检查 |
| 主线计划冻结所有新 ADR/文档修正；项目规则又禁止明知冲突继续实施 | 纠正规则不得冻结 | 改为只冻结新产品范围，遵守既有 ADR 的澄清和前置能力必须先做 | 已解除流程死锁 |
| ADR-0032 要求候选几何修复/剔除/报告，但 B14 未立户 | 不是规则冲突，是交付前置缺口 | 新立 s1-21，并阻塞 s1-07/T19B-6 | B14 尚未实现，M3 不能完成 |
| ADR-0024 原把用料表归 B18；ADR-0026 后改归 B17 | ADR-0026 优先 | 修正 ADR-0017 投影和模块决策索引 | 代码实现需在 M5 再核验 |
| 模块决策索引把导出确认误写成 F2/G2 | F9 承担导出控制台 | 已改为 F9 | 无产品行为变化 |

## 仍需实施验证

- 文档冲突已经消除，不代表 M3 代码已经修好。
- 当前 `xtask/src/arch.rs` 尚不识别 A1，也可能让“依赖合法但运行期由壳串联”的实现通过；A1 立户时补依赖规则，运行期契约缺口归 s1-10 收口。
- B14 完成、A1 接管 M3 编排、S1→A1 契约测试通过之前，69b5b15 不能作为 M3 完成证据。
- 历史 handoff 不逐字改写；它们只用于追溯，不得覆盖本审计的判定顺序。
