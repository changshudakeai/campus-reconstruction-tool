# Build a Modern App Shell

The First Vertical Slice will use a new Modern App Shell rather than continuing directly inside the old static Web and Python UI. Existing code for Sponge v2 export, map-derived data, and Three.js preview remains valuable, but the product should be organized around the two-mode workflow instead of inheriting the old prototype's shape.
