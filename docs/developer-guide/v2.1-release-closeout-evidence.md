# V2.1 版本收口——期限标记归零与完整门禁证据归档

状态：V2.1 收口实施窗口（2026-08-18）。所有 Cargo 命令在 Windows PowerShell 下经
`scripts/cargo-managed.ps1` 执行；UI 测试以 `SLINT_BACKEND=software` 运行。

收口改动绑定：门禁运行于 `HEAD = 3411349adb652e09592b50d7fb98bec74a3ed33b`（含 T49
`a4867d6` 与 T49 标记提交）+ 当时的未提交收口改动
（`git diff HEAD` 指纹 `cb31169bb679532964ebce5d6bad060f62ec9034`，4 文件 +12/-9）。
收口改动随后随 `7bd40cf`、`61256a3` 与本文档提交并入本地 main，并推送 `origin/main`。

## 一、期限标记归零审计

代码中三处 `v2.1.0 / 2026-12-31` 有期限豁免，现已全部归零：

| 位置 | 处置 | 结果 |
|------|------|------|
| `apps/desktop/src/production/mod.rs:4` | T49（`a4867d6`）抽出 `navigation.rs` 与 `bindings.rs`，mod.rs 1891 → 988 行 | 标记删除 |
| `apps/desktop/src/production/workspace_adapter.rs:9` | 本次收口：删除失效期限，转为可持续理由（“小入口、大实现”深适配器；地图 IPC 已由地图会话 `map_session` 集中拥有，T46/ADR-0045） | 标记删除 |
| `core/gaode-client/src/boundary_edit_map_page.rs:25` | 本次收口：同上（小接口 `BoundaryEditPageConfig` + `build_boundary_edit_page_html` 背后承载完整 HTML/JS，不机械拆分） | 标记删除 |

审计命令：`rg "v2\.1\.0|2026-12-31" apps core -g "*.rs"` → 0 命中。

历史文档（`m5-release-closeout-evidence.md`、`docs/history/handoffs/handoff-m5-release-closeout.md`）
保留为历史追溯，不改写。

## 二、T49 完成情况（本地 main `a4867d6`，已并入）

- 新增 `apps/desktop/src/production/navigation.rs`：历史栈/返回/离开确认/取消/回退策略，
  输入当前屏幕、导航意图与工作区入口的离开安全判定，输出结构化转移结果；单测 7 个
  （确认路由/前后向区分/压栈记录/stackable/需确认停留/栈空回退/工作区呈现不再二次决策）。
- 新增 `apps/desktop/src/production/bindings.rs`：机械接线（UI 回调绑定、弹窗调度、
  轮询定时器）仍在组合根模块内。
- `workspace_leave.rs` 离开安全语义未改动；`map_session` 未改动。
- 行为契约：`s1_13_leave_workspace_confirmation`、`s1_31_toolbar_navigation_back_stack`、
  `s1_21_notification_tutorial_contract` 全部通过。

## 三、完整门禁结果（2026-08-18）

| # | 命令 | 结果 |
|---|------|------|
| 1 | `.\scripts\cargo-managed.ps1 -- machete` | 通过（首轮发现并清理 collection-flow 残留未使用 `uuid` 声明；`Cargo.lock` 同步移除该依赖边） |
| 2 | `.\scripts\cargo-managed.ps1 -- test --workspace` | 通过（`SLINT_BACKEND=software`；全部 crate 单测/集成测试/契约测试与 xtask 执法测试全绿） |
| 3 | `.\scripts\cargo-managed.ps1 -- fmt --all --check` | 通过 |
| 4 | `.\scripts\cargo-managed.ps1 -- clippy --workspace --all-targets -- -D warnings` | 通过（0 警告，6m00s） |
| 5 | `.\scripts\cargo-managed.ps1 -- deny check advisories bans licenses sources` | 通过（advisories / bans / licenses / sources 全 ok） |
| 6 | `.\scripts\cargo-managed.ps1 -- xtask ci` | 通过（tidy：行数红线/模块文档/半成品禁令；arch：依赖图符合 ADR-0017） |
| 7 | `.\scripts\cargo-managed.ps1 -- xtask timings` | 通过（11m23s；报告 `target/cargo-timings/cargo-timing-20260818T023155664Z-*.html`）；警告：desktop-shell 编译单元 125.3s 超 120s 预算（ADR-0017 提示项，非阻塞；T49 为同 crate 私有搬分，未改变 crate 拓扑） |

## 四、环境说明

- 完整 workspace 测试并行运行时，`s1_30_fetch_stability`（首轮 exit 0xc000041d）与
  `s1_22_review_scroll_contract`（次轮滚动断言）各出现一次环境性失败；两者单独重跑
  均通过，`SLINT_BACKEND=software` 下完整串行跑通过。判定为并行负载下 Slint/winit
  测试进程的资源抖动，与本次改动无关（本次改动仅注释与未使用依赖清理）。
- 缓存按仓库策略自动回收（timings 后总缓存 40.46 GiB 触发回收），首次重建可能较慢；
  未手工清理任何缓存。

## 五、本次收口改动摘要（已提交并推送）

- `apps/collection-flow/Cargo.toml` + `Cargo.lock`：删除未使用 `uuid` 依赖（提交 `7bd40cf`）。
- `apps/desktop/src/production/workspace_adapter.rs`、`core/gaode-client/src/boundary_edit_map_page.rs`：
  失效期限标记转为可持续理由（纯注释，提交 `61256a3`）。
- 本文档：完整门禁收口证据归档（提交后随 main 一并推送）。

## 六、剩余事项

- 后续候选评估（另行决策，不自动开工）：ADR-0017 crate 清单调整、T26/T27、
  导外交付生命周期深化（原 T42 候选）、生成效果精修、Overture。
- V2.1 真机人工验收：建议从首次设置到边界确认与基础/增强导出的端到端剧本抽查一次。
